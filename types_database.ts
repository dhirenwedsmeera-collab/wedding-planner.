// This is a lightweight placeholder so imports resolve out of the box.
// Once your Supabase project is running, generate real types with:
//
//   npx supabase gen types typescript --project-id YOUR_PROJECT_REF > types/database.ts
//
// That command reads your live schema (all tables/enums from the
// migrations in /supabase/migrations) and produces full type-safety
// for every supabase.from('table') call. Until then, this `any`-based
// shape keeps the app compiling.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
